
import "./UserCard.css";
import UserExpenses from "./UserExpenses";

const UserCard = (props) => {

    // const userName = props.item.userName;
    // const purchaseItem = props.item.purchaseItem;
    // const itemPrice = props.item.itemPrice;

    return (
            <div className="user-card">
                {/* <h3>{userName}</h3>
                <UserExpenses 
                    purchaseItem={purchaseItem}
                    itemPrice={itemPrice}
                /> */}
            </div>
    );
}

export default UserCard;